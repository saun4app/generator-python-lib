language: python

env:
    - TOXENV=py33
    - TOXENV=py34
    - TOXENV=pypy
    - TOXENV=lint
    - TOXENV=pep8

install:
    - pip install -r requirements-dev.txt

os:
    - linux

script:
    - python setup.py test

sudo: false
